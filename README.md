# Restaurant homepage MVP (Next.js + Tailwind)

This is a minimal scaffold for a restaurant homepage MVP using Next.js + Tailwind CSS and TypeScript.

What’s included:
- Minimal Next.js + TypeScript setup
- Tailwind CSS global styles
- Simple components: Hero, Menu, Hours, Footer

Next steps:
1. Run `npm install` in the project root.
2. Run `npm run dev` to start the dev server on http://localhost:3000

Assumptions:
- You want a code-based site (Next.js + Tailwind) and are okay with TypeScript.
# Testing auto-PR workflow fix
